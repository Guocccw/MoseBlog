{
	"compilerOptions": {
		"target": "ES2020",
		"module": "ESNext",
		"moduleResolution": "Node",
		"esModuleInterop": true,
		"resolveJsonModule": true,
		"allowJs": true,
		"types": ["node"], // ← 加上这行
		"baseUrl": "."
	},
	"include": ["scripts/**/*.ts"] // ← 确保包含你的脚本文件
}
